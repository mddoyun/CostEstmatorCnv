# 2025-11-04: BOQ 개선사항 3가지

## 작업 개요

상세견적(DD) 탭의 사용성을 개선하기 위해 3가지 주요 기능을 구현했습니다:
1. 스플릿바 스타일을 3D 뷰포트와 동일한 스타일로 변경
2. 집계표 총계 행에 금액 합산 추가
3. 포함된 산출항목 테이블에 3D 뷰어 연동 열 추가

## 1. 스플릿바 스타일 변경

### 목적
기존의 단순한 스플릿바를 3D 뷰포트에서 사용하는 더 세련된 스타일로 변경하여 시각적 일관성 확보

### 변경 사항

**파일**: `connections/static/connections/style.css` (lines 2655-2694)

**이전 스타일**:
- 4px 단색 배경
- 간단한 아이콘

**새 스타일**:
- 8px 그라데이션 배경
- 수직 점(⋮) 아이콘
- 호버 시 파란색 그라데이션 효과

```css
.boq-split-bar {
    width: 8px;
    background: linear-gradient(to right,
        transparent 0%,
        rgba(0, 0, 0, 0.05) 20%,
        rgba(0, 0, 0, 0.1) 50%,
        rgba(0, 0, 0, 0.05) 80%,
        transparent 100%);
    cursor: col-resize;
    position: relative;
    flex-shrink: 0;
    transition: all 0.2s ease;
    user-select: none;
}

.boq-split-bar:hover {
    background: linear-gradient(to right,
        transparent 0%,
        rgba(0, 120, 212, 0.1) 20%,
        rgba(0, 120, 212, 0.2) 50%,
        rgba(0, 120, 212, 0.1) 80%,
        transparent 100%);
}

.boq-split-bar::before {
    content: '⋮';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 16px;
    color: rgba(0, 0, 0, 0.3);
    pointer-events: none;
    letter-spacing: -2px;
}

.boq-split-bar:hover::before {
    color: var(--primary-color);
}
```

### 효과
- 더 넓은 드래그 영역 (4px → 8px)
- 시각적으로 더 명확한 경계 표시
- 3D 뷰포트와 일관된 디자인

---

## 2. 집계표 총계 행 금액 합산

### 문제점
집계표 하단의 "총계" 행에서 재료비, 노무비, 경비, 합계금액이 집계되지 않고 "-"로 표시됨

### 원인
백엔드에서 보내는 summary 데이터의 필드명이 프론트엔드에서 기대하는 것과 다름:
- 백엔드: `total_material_cost`, `total_labor_cost` 등
- 프론트엔드: `material_cost_total`, `labor_cost_total` 등

### 해결 방법

**파일**: `connections/views.py` (lines 2779-2786)

**변경 전**:
```python
total_summary = {
    'total_quantity': format_quantity(sum(...)),
    'total_count': len(items),
    'total_material_cost': format_cost(sum(...)),
    'total_labor_cost': format_cost(sum(...)),
    'total_expense_cost': format_cost(sum(...)),
    'total_total_cost': format_cost(sum(...)),
}
```

**변경 후**:
```python
total_summary = {
    'quantity': format_quantity(sum((item['quantity'] for item in items), start=ZERO_DECIMAL)),
    'count': len(items),
    'material_cost_total': format_cost(sum((item['material_cost_total'] for item in items), start=ZERO_DECIMAL)),
    'labor_cost_total': format_cost(sum((item['labor_cost_total'] for item in items), start=ZERO_DECIMAL)),
    'expense_cost_total': format_cost(sum((item['expense_cost_total'] for item in items), start=ZERO_DECIMAL)),
    'total_cost_total': format_cost(sum((item['total_cost_total'] for item in items), start=ZERO_DECIMAL)),
}
```

### 결과
- 총계 행에 모든 산출항목의 재료비 합계 표시
- 총계 행에 모든 산출항목의 노무비 합계 표시
- 총계 행에 모든 산출항목의 경비 합계 표시
- 총계 행에 모든 산출항목의 합계금액 표시
- 수량과 항목 수도 정상적으로 표시

---

## 3. 3D 뷰어 연동 열 추가

### 목적
포함된 산출항목 테이블에서 BIM 저작도구 연동 외에도 3D 뷰어 연동 기능을 제공

### 구현 내용

#### 3.1 테이블 헤더 추가

**파일**: `connections/static/connections/boq_detailed_estimation_handlers.js` (lines 615-631)

```javascript
const headers = [
    { id: "cost_code_name", label: "산출항목" },
    { id: "quantity", label: "수량", align: "right" },
    { id: "unit_price_type_name", label: "단가기준", align: "center" },
    { id: "total_cost_unit", label: "합계단가", align: "right" },
    { id: "material_cost_unit", label: "재료비단가", align: "right" },
    { id: "labor_cost_unit", label: "노무비단가", align: "right" },
    { id: "expense_cost_unit", label: "경비단가", align: "right" },
    { id: "total_cost_total", label: "합계금액", align: "right" },
    { id: "material_cost_total", label: "재료비", align: "right" },
    { id: "labor_cost_total", label: "노무비", align: "right" },
    { id: "expense_cost_total", label: "경비", align: "right" },
    { id: "linked_member_name", label: "연관 부재" },
    { id: "linked_raw_name", label: "BIM 원본 객체" },
    { id: "viewer_sync", label: "3D 뷰어", align: "center" }, // ← 새로 추가
    { id: "actions", label: "BIM 연동", align: "center" },
];
```

#### 3.2 버튼 렌더링

**파일**: `connections/static/connections/boq_detailed_estimation_handlers.js` (lines 683-685)

```javascript
viewer_sync: rawElement
    ? `<button class="select-in-viewer-btn-detail" data-cost-item-id="${item.id}" title="3D 뷰어에서 선택 확인">🎯</button>`
    : "",
```

- **버튼 클래스**: `select-in-viewer-btn-detail`
- **아이콘**: 🎯 (타겟 이모지)
- **툴팁**: "3D 뷰어에서 선택 확인"
- **조건**: BIM 객체가 있는 경우에만 표시

#### 3.3 이벤트 핸들러 추가

**파일**: `connections/static/connections/boq_detailed_estimation_handlers.js` (lines 477-491)

기존 이벤트 리스너에 3D 뷰어 버튼 처리 로직 추가:

```javascript
itemListContainer.addEventListener("click", (e) => {
    const itemRow = e.target.closest("tr[data-item-id]");
    const bimButton = e.target.closest("button.select-in-client-btn-detail");
    const viewerButton = e.target.closest("button.select-in-viewer-btn-detail");

    if (viewerButton) {
        const costItemId = viewerButton.dataset.costItemId;
        console.log(`[DEBUG][Event] 3D Viewer link button clicked for CostItem ID: ${costItemId}`);
        handleBoqSelectInViewerFromDetail(costItemId);
    } else if (bimButton) {
        const costItemId = bimButton.dataset.costItemId;
        console.log(`[DEBUG][Event] BIM link button clicked for CostItem ID: ${costItemId}`);
        handleBoqSelectInClientFromDetail(costItemId);
    } else if (itemRow) {
        // ... 기존 행 클릭 로직
    }
});
```

#### 3.4 핸들러 함수 구현

**파일**: `connections/static/connections/boq_detailed_estimation_handlers.js` (lines 587-631)

```javascript
/**
 * BOQ 하단 상세 목록 테이블의 '3D 뷰어' 버튼 클릭 시 호출됩니다.
 * @param {string} costItemId - 클릭된 버튼이 속한 CostItem의 ID
 */
function handleBoqSelectInViewerFromDetail(costItemId) {
    console.log(`[DEBUG] handleBoqSelectInViewerFromDetail called for CostItem ID: ${costItemId}`);
    if (!costItemId) return;

    // CostItem → QuantityMember → RawElement 찾기
    const costItem = (loadedDdCostItems || loadedCostItems || []).find((ci) => ci.id === costItemId);
    const member = costItem?.quantity_member_id
        ? loadedQuantityMembers.find((qm) => qm.id.toString() === costItem.quantity_member_id.toString())
        : null;
    const rawElement = member?.raw_element_id
        ? allRevitData.find((re) => re.id.toString() === member.raw_element_id.toString())
        : null;

    if (!rawElement || !rawElement.id) {
        showToast("이 항목과 연동된 BIM 객체를 찾을 수 없습니다.", "warning");
        console.warn(`[DEBUG] Could not find linked RawElement for CostItem ID: ${costItemId}`);
        return;
    }

    console.log(`[DEBUG] Found RawElement ID to select in viewer: ${rawElement.id}`);

    // 3D 뷰어에서 객체 선택 (three_d_viewer.js의 함수 사용)
    if (typeof selectObjectsInViewer === 'function') {
        selectObjectsInViewer([rawElement.id]);
        showToast("3D 뷰어에서 객체를 선택했습니다.", "success");
        console.log(`[DEBUG] Selected object ${rawElement.id} in 3D viewer.`);

        // 3D 뷰어 탭으로 자동 전환
        if (typeof switchTab === 'function') {
            switchTab('three-d-viewer');
        }
    } else {
        showToast("3D 뷰어 기능을 사용할 수 없습니다.", "error");
        console.error("[ERROR] selectObjectsInViewer function not found.");
    }
}
```

### 기능 설명

1. **CostItem → RawElement 관계 추적**:
   - CostItem의 `quantity_member_id`로 QuantityMember 찾기
   - QuantityMember의 `raw_element_id`로 RawElement 찾기

2. **3D 뷰어 객체 선택**:
   - `selectObjectsInViewer()` 함수 호출 (three_d_viewer.js에 정의됨)
   - RawElement ID 배열 전달

3. **자동 탭 전환**:
   - `switchTab('three-d-viewer')` 호출
   - 3D 뷰어 탭으로 자동 이동하여 선택된 객체 확인 가능

4. **사용자 피드백**:
   - 성공 시: "3D 뷰어에서 객체를 선택했습니다." 토스트 메시지
   - 실패 시: "이 항목과 연동된 BIM 객체를 찾을 수 없습니다." 경고 메시지

### 사용 시나리오

1. **견적 > 상세견적(DD)** 탭에서 집계표 행 클릭
2. 하단 "포함된 산출항목" 테이블에 해당 그룹의 항목들 표시
3. 특정 산출항목의 "3D 뷰어" 열에 있는 🎯 버튼 클릭
4. 자동으로 3D 뷰어 탭으로 전환
5. 해당 BIM 객체가 3D 뷰어에서 선택됨 (하이라이트)

### BIM 연동 vs 3D 뷰어 비교

| 기능 | BIM 연동 (👁️) | 3D 뷰어 (🎯) |
|------|---------------|-------------|
| 대상 | Revit/Blender 저작도구 | 웹 3D 뷰어 |
| 통신 | WebSocket | 로컬 JavaScript 함수 호출 |
| 반응 속도 | 네트워크 지연 있음 | 즉시 반응 |
| 필요 조건 | Revit/Blender 실행 필요 | BIM 데이터만 로드되면 가능 |
| 주 용도 | 저작도구에서 수정 | 빠른 시각적 확인 |

---

## 파일 변경 목록

### 1. connections/static/connections/style.css
- **Line 2655-2694**: 스플릿바 스타일 개선 (3D 뷰포트 스타일 적용)

### 2. connections/views.py
- **Line 2779-2786**: 집계표 총계 필드명 수정

### 3. connections/static/connections/boq_detailed_estimation_handlers.js
- **Line 615-631**: 포함된 산출항목 테이블 헤더에 "3D 뷰어" 열 추가
- **Line 683-685**: 3D 뷰어 버튼 렌더링
- **Line 477-491**: 이벤트 리스너에 3D 뷰어 버튼 처리 추가
- **Line 587-631**: `handleBoqSelectInViewerFromDetail()` 함수 구현

---

## 테스트 방법

### 1. 스플릿바 테스트
1. **견적 > 상세견적(DD)** 탭 이동
2. 좌측-중간, 중간-우측 사이의 스플릿바 확인
3. 호버 시 파란색 그라데이션 효과 확인
4. 드래그하여 패널 크기 조정 확인
5. 수직 점(⋮) 아이콘 표시 확인

### 2. 총계 금액 집계 테스트
1. **견적 > 상세견적(DD)** 탭에서 집계표 생성
2. 테이블 하단 "총계" 행 확인
3. "재료비", "노무비", "경비", "합계금액" 열에 숫자가 표시되는지 확인
4. 각 열의 합계가 정확한지 검증

### 3. 3D 뷰어 연동 테스트
1. **견적 > 상세견적(DD)** 탭에서 집계표 행 클릭
2. 하단 "포함된 산출항목" 테이블 확인
3. "3D 뷰어" 열에 🎯 버튼 확인
4. 🎯 버튼 클릭
5. 3D 뷰어 탭으로 자동 전환 확인
6. 해당 객체가 3D 뷰어에서 선택(하이라이트)되는지 확인

---

## 주의사항

1. **3D 뷰어 기능 의존성**:
   - `selectObjectsInViewer()` 함수는 `three_d_viewer.js`에 정의되어 있어야 함
   - `switchTab()` 함수는 `navigation.js`에 정의되어 있어야 함
   - 함수가 없으면 에러 메시지 표시

2. **BIM 객체 필터링**:
   - 3D 뷰어 버튼은 `rawElement`가 있는 경우에만 표시
   - 연관 BIM 객체가 없는 산출항목은 빈 셀로 표시

3. **총계 계산**:
   - Decimal 타입을 사용하여 정확한 금액 계산
   - 프론트엔드에서는 천단위 구분자로 표시

---

## 개선 효과

1. **시각적 일관성**: 스플릿바가 3D 뷰포트와 동일한 스타일로 통일감 증대
2. **정보 정확성**: 총계 행에 금액이 정확히 표시되어 전체 예산 파악 용이
3. **작업 효율성**: 3D 뷰어로 빠르게 객체 확인 가능, 저작도구 전환 불필요
4. **사용자 경험**: 두 가지 시각화 방법(저작도구/웹 뷰어) 선택 가능

---

## 관련 커밋 메시지

```
Improve BOQ UI with 3 enhancements

1. Update split bar style to match 3D viewport design
   - Change width from 4px to 8px
   - Add gradient background with vertical dots icon
   - Improve hover effects with blue gradient

2. Fix BOQ summary row cost totals
   - Rename backend summary fields to match frontend expectations
   - Display material_cost_total, labor_cost_total, expense_cost_total
   - Show total_cost_total in summary row

3. Add 3D viewer sync column to cost items table
   - Add "3D 뷰어" column with 🎯 button
   - Implement handleBoqSelectInViewerFromDetail() function
   - Auto-switch to 3D viewer tab on button click
   - Select and highlight corresponding BIM object in viewer
```
