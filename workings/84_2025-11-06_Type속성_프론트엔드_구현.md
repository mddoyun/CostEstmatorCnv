# 84_2025-11-06_Type속성_프론트엔드_구현.md

## 작업 일자
2025-11-06

## 작업 목표
IFC Type 속성 (Type.Attributes, Type.PropertySet)을 프론트엔드에서 표시하고 사용할 수 있도록 구현

## 선행 작업
- [83_2025-11-06_IFC_Type속성_및_조건부_숨김_기능_설계.md](./83_2025-11-06_IFC_Type속성_및_조건부_숨김_기능_설계.md): 전체 설계 문서
- Blender Addon 수정 완료 (Type.Attributes, Type.PropertySet 수집)

## 작업 내용

### 1. 프론트엔드 Type 속성 표시 기능 구현

#### 1.1. `ui.js` - Property Options 생성 함수 확장

**파일**: `connections/static/connections/ui.js`

**수정 위치 1**: `generateBIMPropertyOptions()` 함수 (라인 3470~3665)

```javascript
// 각 카테고리별로 필드 수집
const systemProps = new Set();
const attributeProps = new Set();
const instanceParams = new Set();
const typeParams = new Set();
const quantitySetProps = new Set();
const propertySetProps = new Set();
const spatialContainerProps = new Set();
const typeInfoProps = new Set();
const typeAttributeProps = new Set(); // ✅ 추가
const typePropertySetProps = new Set(); // ✅ 추가
const otherProps = new Set();
```

**수정 위치 2**: Type 속성 분류 로직 (라인 3531~3542)

```javascript
// Type.Attributes.* 형태의 속성 (세부 타입 속성)
else if (k.startsWith('Type.Attributes.')) {
    typeAttributeProps.add(k);
}
// Type.PropertySet.* 형태의 속성 (타입 PropertySet)
else if (k.startsWith('Type.PropertySet.')) {
    typePropertySetProps.add(k);
}
// Type.* 형태의 속성 (기본 타입 정보)
else if (k.startsWith('Type.')) {
    typeInfoProps.add(k);
}
```

**수정 위치 3**: Property Option 그룹 표시 (라인 3638~3662)

```javascript
// BIM.Type.Attributes.* 그룹 (타입 세부 속성 - 2025-11-06 추가)
if (typeAttributeProps.size > 0) {
    const options = Array.from(typeAttributeProps).sort().map(prop => {
        const displayName = `BIM.${prop}`;
        return { value: displayName, label: displayName };
    });
    propertyOptions.push({
        group: 'BIM Type Attributes (타입 세부 속성)',
        options: options
    });
}

// BIM.Type.PropertySet.* 그룹 (타입 PropertySet - 2025-11-06 추가)
if (typePropertySetProps.size > 0) {
    const options = Array.from(typePropertySetProps).sort().map(prop => {
        const displayName = `BIM.${prop}`;
        return { value: displayName, label: displayName };
    });
    propertyOptions.push({
        group: 'BIM Type PropertySet (타입 속성 세트)',
        options: options
    });
}
```

#### 1.2. `ui.js` - Property 값 접근 함수 확장

**수정 위치**: `getValueForItem()` 함수 (라인 135~225)

```javascript
// ▼▼▼ [추가] 다단계 경로 처리 (Type.Attributes.*, Type.PropertySet.*, etc.) - 2025-11-06 ▼▼▼
// 경로가 점으로 구분된 경우 (e.g., "Type.Attributes.Name", "PropertySet.Pset_WallCommon__LoadBearing")
if (internalField.includes('.')) {
    const parts = internalField.split('.');
    let current = raw_data;

    for (const part of parts) {
        if (current && typeof current === 'object' && part in current) {
            current = current[part];
        } else {
            current = undefined;
            break;
        }
    }

    if (current !== undefined && current !== null) {
        // 객체인 경우 JSON 문자열로 반환
        if (typeof current === 'object') {
            return JSON.stringify(current);
        }
        return current;
    }
}
// ▲▲▲ [추가] 여기까지 ▲▲▲
```

**설명**:
- Type.Attributes.Name → raw_data['Type']['Attributes']['Name'] 경로로 접근
- Type.PropertySet.Pset_WallCommon__LoadBearing → raw_data['Type']['PropertySet']['Pset_WallCommon__LoadBearing']
- 다단계 객체 경로를 동적으로 탐색하여 값 추출

#### 1.3. Helper Panel (도우미 패널) 확장

**파일**: `connections/static/connections/data_management_handlers.js`

**수정 위치 1**: Property Groups 정의 (라인 503~515)

```javascript
const propertyGroups = {
    'System': [],
    'Attributes': [],
    'Parameters': [],
    'TypeParameters': [],
    'QuantitySet': [],
    'TypeInfo': [],            // Type.Name, Type.IfcClass 등
    'TypeAttributes': [],      // Type.Attributes.* 추가 (2025-11-06)
    'TypePropertySet': [],     // Type.PropertySet.* 추가 (2025-11-06)
    'Other': []
};
```

**수정 위치 2**: Type 속성 분류 로직 (라인 572~607)

```javascript
// ▼▼▼ [추가] Type 그룹 처리 (2025-11-06) ▼▼▼
// Type 객체 처리 (Type.Name, Type.IfcClass, Type.Attributes.*, Type.PropertySet.*)
else if (topLevelKey === 'Type' && typeof topLevelValue === 'object' && !Array.isArray(topLevelValue)) {
    Object.entries(topLevelValue).forEach(([typeKey, typeValue]) => {
        // Type.Attributes.* 그룹
        if (typeKey === 'Attributes' && typeof typeValue === 'object' && !Array.isArray(typeValue)) {
            Object.entries(typeValue).forEach(([attrKey, attrValue]) => {
                if (attrValue !== undefined && attrValue !== null) {
                    propertyGroups['TypeAttributes'].push({
                        displayKey: `{Type.Attributes.${attrKey}}`,
                        value: String(attrValue)
                    });
                }
            });
        }
        // Type.PropertySet.* 그룹
        else if (typeKey === 'PropertySet' && typeof typeValue === 'object' && !Array.isArray(typeValue)) {
            Object.entries(typeValue).forEach(([propKey, propValue]) => {
                if (propValue !== undefined && propValue !== null) {
                    propertyGroups['TypePropertySet'].push({
                        displayKey: `{Type.PropertySet.${propKey}}`,
                        value: String(propValue)
                    });
                }
            });
        }
        // Type.Name, Type.IfcClass 등 기본 정보
        else if (typeValue !== undefined && typeValue !== null) {
            propertyGroups['TypeInfo'].push({
                displayKey: `{Type.${typeKey}}`,
                value: String(typeValue)
            });
        }
    });
}
// ▲▲▲ [추가] 여기까지 ▲▲▲
```

**수정 위치 3**: Group Configs 확장 (라인 630~641)

```javascript
const groupConfigs = [
    { key: 'System', title: '⚙️ 시스템 속성', color: '#1976d2' },
    { key: 'Attributes', title: '🏗️ 기본 속성', color: '#388e3c' },
    { key: 'Parameters', title: '🔧 Parameters', color: '#f57c00' },
    { key: 'TypeParameters', title: '📝 TypeParameters', color: '#7b1fa2' },
    { key: 'QuantitySet', title: '📏 QuantitySet', color: '#0288d1' },
    { key: 'TypeInfo', title: '🏷️ Type Info', color: '#c2185b' },              // ✅ 추가
    { key: 'TypeAttributes', title: '🔖 Type Attributes', color: '#e91e63' },   // ✅ 추가
    { key: 'TypePropertySet', title: '📋 Type PropertySet', color: '#ad1457' }, // ✅ 추가
    { key: 'Other', title: '📦 기타 속성', color: '#607d8b' }
];
```

### 2. Database Model 확장 (Visibility Filters)

**파일**: `connections/models.py`

**수정 내용**: Project 모델에 `visibility_filters` 필드 추가 (라인 19)

```python
class Project(models.Model):
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    name = models.CharField(max_length=200, default="새 프로젝트")
    description = models.TextField(blank=True, null=True)
    created_at = models.DateTimeField(auto_now_add=True)
    visibility_filters = models.JSONField(default=list, blank=True)  # 추가 (2025-11-06)
```

**Migration 생성 및 적용**:

```bash
.mddoyun/bin/python manage.py makemigrations connections
# Migrations for 'connections':
#   connections/migrations/0024_project_visibility_filters.py
#     + Add field visibility_filters to project

.mddoyun/bin/python manage.py migrate connections
# Operations to perform:
#   Apply all migrations: connections
# Running migrations:
#   Applying connections.0024_project_visibility_filters... OK
```

## 구현 완료 항목

### ✅ Phase 4: Frontend Type Properties Display (완료)

1. ✅ `generateBIMPropertyOptions()` 함수 확장
   - Type.Attributes.* 속성 수집
   - Type.PropertySet.* 속성 수집
   - 필드 선택 UI에 자동 표시

2. ✅ `getValueForItem()` 함수 확장
   - 다단계 경로 접근 지원 (점 표기법)
   - Type.Attributes.Name, Type.PropertySet.Pset_xxx__Property 등 접근 가능

3. ✅ Helper Panel 확장
   - Type Info 그룹 (🏷️ Type Info)
   - Type Attributes 그룹 (🔖 Type Attributes)
   - Type PropertySet 그룹 (📋 Type PropertySet)
   - 선택한 객체의 Type 속성 실시간 표시

4. ✅ Database Model 확장
   - Project.visibility_filters 필드 추가
   - Migration 생성 및 적용 완료

## 미완료 항목 (다음 작업)

### 🔲 Phase 5: Conditional Visibility UI (진행 중)

**필요한 작업**:

1. **UI 추가** (revit_control.html):
   - "3D 뷰어 표시 설정" 섹션 추가
   - 필터 목록 표시 테이블
   - 필터 추가/수정/삭제 버튼
   - 필터 조건 빌더 (Condition Builder 패턴 재사용)

2. **JavaScript 핸들러 작성** (visibility_filter_handlers.js 신규 생성):
   - `loadVisibilityFilters()`: 프로젝트의 필터 목록 로드
   - `renderVisibilityFilterTable()`: 필터 테이블 렌더링
   - `addVisibilityFilter()`: 새 필터 추가
   - `editVisibilityFilter()`: 필터 수정
   - `deleteVisibilityFilter()`: 필터 삭제
   - `applyVisibilityFilters()`: 3D 뷰어에 필터 적용

3. **3D Viewer 통합** (three_d_viewer.js):
   - `loadBimGeometry()` 함수에서 visibility_filters 확인
   - 조건 매칭 함수 작성 (evaluateCondition)
   - 숨김 처리 로직 구현 (mesh.visible = false)

4. **Backend API 엔드포인트**:
   - `GET /api/projects/{id}/visibility-filters/`: 필터 조회
   - `POST /api/projects/{id}/visibility-filters/`: 필터 저장
   - `DELETE /api/projects/{id}/visibility-filters/{filter_id}/`: 필터 삭제

### 🔲 Phase 6: Revit Addin Update (대기 중)

**필요한 작업**:

1. **RevitDataCollector.cs 수정**:
   - Element.GetTypeId() 호출하여 ElementType 가져오기
   - Type Parameters 수집
   - Type PropertySets 수집 (HasPropertySets)

2. **JSON 구조 통일**:
   - Blender와 동일한 Type.Attributes, Type.PropertySet 구조 사용

3. **테스트**:
   - Revit 샘플 프로젝트에서 Type 데이터 수집 확인
   - 프론트엔드에서 표시 확인

### 🔲 Phase 7: Testing (대기 중)

**테스트 시나리오**:

1. **Blender IFC 데이터 테스트**:
   - IFC 파일 로드
   - Type.Attributes, Type.PropertySet 표시 확인
   - Helper Panel에서 Type 속성 확인
   - Ruleset에서 Type 속성 사용 테스트

2. **Revit 데이터 테스트** (Revit Addin 완료 후):
   - Revit 프로젝트 연결
   - Type Parameters 표시 확인
   - 혼합 데이터 (Revit + IFC) 동작 확인

3. **Visibility Filters 테스트** (UI 완료 후):
   - 필터 생성/수정/삭제
   - 3D 뷰어에서 객체 숨김/표시
   - 필터 조건 복합 테스트

## 데이터 구조 예시

### Blender에서 수집된 Type 데이터 (raw_data)

```json
{
  "Name": "Basic Wall",
  "IfcClass": "IfcWall",
  "Type": {
    "Name": "Basic Wall: Generic - 200mm",
    "IfcClass": "IfcWallType",
    "Attributes": {
      "Name": "Basic Wall: Generic - 200mm",
      "Description": "Standard concrete wall",
      "ApplicableOccurrence": null,
      "ElementType": "SOLIDWALL",
      "Tag": "W-001"
    },
    "PropertySet": {
      "Pset_WallTypeCommon__ThermalTransmittance": 2.5,
      "Pset_WallTypeCommon__LoadBearing": true,
      "Pset_WallTypeCommon__FireRating": "F-90"
    }
  },
  "Parameters": {
    "Length": 5000.0,
    "Height": 3000.0
  }
}
```

### 필드 선택 UI 표시

**BIM Type Attributes (타입 세부 속성)** 그룹:
- `BIM.Type.Attributes.Name`
- `BIM.Type.Attributes.Description`
- `BIM.Type.Attributes.ElementType`
- `BIM.Type.Attributes.Tag`

**BIM Type PropertySet (타입 속성 세트)** 그룹:
- `BIM.Type.PropertySet.Pset_WallTypeCommon__ThermalTransmittance`
- `BIM.Type.PropertySet.Pset_WallTypeCommon__LoadBearing`
- `BIM.Type.PropertySet.Pset_WallTypeCommon__FireRating`

### Helper Panel 표시 예시

**🏷️ Type Info**:
| Property | Value |
|----------|-------|
| `{Type.Name}` | Basic Wall: Generic - 200mm |
| `{Type.IfcClass}` | IfcWallType |

**🔖 Type Attributes**:
| Property | Value |
|----------|-------|
| `{Type.Attributes.Description}` | Standard concrete wall |
| `{Type.Attributes.ElementType}` | SOLIDWALL |
| `{Type.Attributes.Tag}` | W-001 |

**📋 Type PropertySet**:
| Property | Value |
|----------|-------|
| `{Type.PropertySet.Pset_WallTypeCommon__FireRating}` | F-90 |
| `{Type.PropertySet.Pset_WallTypeCommon__LoadBearing}` | true |
| `{Type.PropertySet.Pset_WallTypeCommon__ThermalTransmittance}` | 2.5 |

## 기술적 세부사항

### Property Inheritance Chain

Type 속성은 Property Inheritance System에 따라 모든 하위 엔티티에서 접근 가능:

```
RawElement (BIM.Type.Attributes.*)
  ↓ 상속
QuantityMember (BIM.Type.Attributes.* + QM.*)
  ↓ 상속
CostItem (BIM.Type.Attributes.* + QM.* + CI.*)
  ↓ 상속
ActivityObject (BIM.Type.Attributes.* + QM.* + CI.* + AO.*)
```

### Function Reusability

이번 구현에서 **기존 함수 재사용** 원칙 준수:

1. ✅ `generateBIMPropertyOptions()` 확장 (재작성 ✗)
2. ✅ `getValueForItem()` 다단계 경로 처리 추가 (기존 로직 유지)
3. ✅ `renderRawDataHelperPanel()` 그룹 추가 (기존 패턴 따름)

**결과**:
- 일관된 Property 표시
- 기존 Ruleset 시스템과 호환
- 유지보수 용이

## 다음 단계

### 우선순위 1: Visibility Filters UI 완성

1. `revit_control.html`에 UI 섹션 추가
2. `visibility_filter_handlers.js` 작성
3. Backend API 엔드포인트 구현
4. 3D Viewer 통합

### 우선순위 2: Revit Addin 업데이트

1. RevitDataCollector.cs 수정
2. JSON 구조 통일
3. 테스트 및 검증

### 우선순위 3: 통합 테스트

1. Blender + Revit 혼합 데이터 테스트
2. Ruleset에서 Type 속성 활용 테스트
3. Visibility Filters 성능 테스트

## 참고 문서

- [83_2025-11-06_IFC_Type속성_및_조건부_숨김_기능_설계.md](./83_2025-11-06_IFC_Type속성_및_조건부_숨김_기능_설계.md): 전체 설계 문서
- [CLAUDE.md](../CLAUDE.md): Property Inheritance System 설명
- [workings/81_2025-11-05_홈_대시보드_구현.md](./81_2025-11-05_홈_대시보드_구현.md): 최근 대시보드 작업

## 변경된 파일 목록

### Backend
- `connections/models.py`: Project.visibility_filters 필드 추가
- `connections/migrations/0024_project_visibility_filters.py`: 신규 마이그레이션

### Frontend
- `connections/static/connections/ui.js`: Type 속성 표시 기능 추가
- `connections/static/connections/data_management_handlers.js`: Helper Panel 확장

### Blender Addon (이전 작업)
- `CostEstimator_BlenderAddon_453/__init__.py`: Type.Attributes, Type.PropertySet 수집

## 작업 소요 시간

- Frontend 구현: 약 1.5시간
- Database Model & Migration: 약 15분
- 문서 작성: 약 30분
- **총 소요 시간**: 약 2시간 15분

## 결론

IFC Type 속성 (Type.Attributes, Type.PropertySet)의 프론트엔드 표시 기능을 완료했습니다.

**완료된 기능**:
- ✅ 필드 선택 UI에 Type 속성 자동 표시
- ✅ Helper Panel에 Type 속성 그룹 표시
- ✅ 다단계 Property 경로 접근 지원
- ✅ Database Model 확장 (visibility_filters)

**다음 작업**:
- 🔲 Conditional Visibility UI 구현
- 🔲 Revit Addin Type 속성 수집
- 🔲 통합 테스트

모든 구현은 기존 Property Inheritance System과 호환되며, Function Reusability 원칙을 준수했습니다.
