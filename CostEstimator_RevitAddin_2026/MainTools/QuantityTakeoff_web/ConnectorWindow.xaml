<Window x:Class="RevitDjangoConnector.ConnectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitDjangoConnector"
        mc:Ignorable="d"
        Title="Cost Estimator Connector" Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="StartServerButton" Grid.Row="0" Content="✅ 서버 시작" Margin="0,0,0,10" Click="StartServerButton_Click" Height="30" FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Server URL:" VerticalAlignment="Center"/>
            <TextBox x:Name="ServerUrlTextBox" Grid.Column="1" Text="ws://127.0.0.1:8000/ws/revit-connector/" VerticalAlignment="Center" Margin="5,0"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="ConnectButton" Content="🌐 연결 및 브라우저 열기" Width="180" Margin="5" Click="ConnectButton_Click"/>
            <Button x:Name="DisconnectButton" Content="❌ 연결 끊기 &amp; 서버 종료" Width="180" Margin="5" IsEnabled="False" Click="DisconnectButton_Click"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,5,0,5">
            <ProgressBar x:Name="ProgressBar" Height="20"/>
            <TextBlock x:Name="ProgressTextBlock" Text="Ready" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
        </Grid>

        <TextBox x:Name="StatusTextBox" Grid.Row="4" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                 Text="Ready."/>
    </Grid>
</Window>